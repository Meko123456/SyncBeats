name: iosApp
options:
  deploymentTarget:
    iOS: "16.0"
# Exact versions, not `from:`. The Xcode project is generated rather than committed, and the
# SwiftPM lockfile lives inside it — so a range here would mean every checkout and every CI run
# resolving whatever happens to be newest, with nothing recording what was actually built. These
# are the versions the previously committed Package.resolved had pinned.
packages:
  Firebase:
    url: https://github.com/firebase/firebase-ios-sdk
    exactVersion: 12.16.0
  GoogleSignIn:
    url: https://github.com/google/GoogleSignIn-iOS
    exactVersion: 9.2.0
targets:
  iosApp:
    type: application
    platform: iOS
    sources:
      - iosApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.example.myapplicationmusicsharing
        INFOPLIST_FILE: iosApp/Info.plist
        GENERATE_INFOPLIST_FILE: NO
        FRAMEWORK_SEARCH_PATHS: "$(inherited) $(SRCROOT)/../composeApp/build/xcode-frameworks/$(CONFIGURATION)/$(SDK_NAME)"
        OTHER_LDFLAGS: "$(inherited) -framework ComposeApp"
        ENABLE_USER_SCRIPT_SANDBOXING: NO
        # :composeApp declares iosArm64 and iosSimulatorArm64 and deliberately not iosX64 — no
        # machine here or on CI is Intel. But `-destination 'generic/platform=iOS Simulator'` asks
        # Xcode for every simulator architecture, so it requests x86_64, the Kotlin plugin has no
        # target for it, and the build dies with "Unknown iOS simulator arch: 'x86_64'".
        #
        # Stated once here rather than as a flag every xcodebuild invocation has to remember.
        EXCLUDED_ARCHS[sdk=iphonesimulator*]: x86_64
        # Ad-hoc signing: free, works on simulator, and grants the keychain
        # entitlement Firebase Auth needs to store its session.
        CODE_SIGN_IDENTITY: "-"
    dependencies:
      - package: Firebase
        product: FirebaseAuth
      - package: Firebase
        product: FirebaseDatabase
      - package: GoogleSignIn
        product: GoogleSignIn
    preBuildScripts:
      - name: Compile Kotlin Framework
        script: |
          export JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"
          cd "$SRCROOT/.."
          ./gradlew :composeApp:embedAndSignAppleFrameworkForXcode
        basedOnDependencyAnalysis: false
